# Tips

## Updates

```shell
claude config get --global autoUpdates # Check autoUpdates globally
claude config set --global autoUpdates false  # Disable autoUpdates globally
claude config set --global autoUpdates true # Enable autoUpdates globally
claude update # Update manually
```
